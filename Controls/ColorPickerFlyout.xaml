<UserControl x:Class="FluxionJsSceneEditor.Controls.ColorPickerFlyout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="220" d:DesignWidth="260">
    <Border Padding="10" CornerRadius="6" BorderThickness="1"
            Background="{DynamicResource App.PanelBrush}"
            BorderBrush="{DynamicResource App.BorderBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="70"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Foreground="{DynamicResource App.SubtleTextBrush}" Text="Preview"/>
            <Border Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Height="22" CornerRadius="4" BorderThickness="1"
                    BorderBrush="{DynamicResource App.BorderBrush}" Background="{Binding PreviewBrush, RelativeSource={RelativeSource AncestorType=UserControl}}" />

            <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Foreground="{DynamicResource App.SubtleTextBrush}" Text="Hex" Margin="0,8,0,0"/>
            <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,8,0,0" Padding="6,3"
                     Text="{Binding Hex, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Foreground="{DynamicResource App.SubtleTextBrush}" Text="R" Margin="0,8,0,0"/>
            <Slider Grid.Row="2" Grid.Column="1" Margin="0,8,8,0" Minimum="0" Maximum="255" Value="{Binding R, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            <TextBox Grid.Row="2" Grid.Column="2" Margin="0,8,0,0" Padding="6,3" Text="{Binding R, RelativeSource={RelativeSource AncestorType=UserControl}}"/>

            <TextBlock Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Foreground="{DynamicResource App.SubtleTextBrush}" Text="G" Margin="0,6,0,0"/>
            <Slider Grid.Row="3" Grid.Column="1" Margin="0,6,8,0" Minimum="0" Maximum="255" Value="{Binding G, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            <TextBox Grid.Row="3" Grid.Column="2" Margin="0,6,0,0" Padding="6,3" Text="{Binding G, RelativeSource={RelativeSource AncestorType=UserControl}}"/>

            <TextBlock Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Foreground="{DynamicResource App.SubtleTextBrush}" Text="B" Margin="0,6,0,0"/>
            <Slider Grid.Row="4" Grid.Column="1" Margin="0,6,8,0" Minimum="0" Maximum="255" Value="{Binding B, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            <TextBox Grid.Row="4" Grid.Column="2" Margin="0,6,0,0" Padding="6,3" Text="{Binding B, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </Grid>
    </Border>
</UserControl>
